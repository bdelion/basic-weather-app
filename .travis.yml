# Set the token in Travis environment settings instead defining here.
env:
  global:
    - CODECOV_TOKEN="f095166d-5a9b-4a1a-ada5-97a227937eae"
    - CC_TEST_REPORTER_ID="642ea9f3c58c93cf5dcb6287099228ac278dd9095311feebf2c041756875bd32"
language: java
jdk:
  - oraclejdk8
cache:
  directories:
    - $HOME/.m2
install: true

#For Code Climate Test Coverage
before_script:
  - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
  - chmod +x ./cc-test-reporter
  - ./cc-test-reporter before-build

script:
  - mvn clean install

after_script:
  #TODEL - ./cc-test-reporter after-build -t jacoco -p target/site/jacoco/jacoco.xml -d --exit-code $TRAVIS_TEST_RESULT
  #TODEL - ./cc-test-reporter after-build -d -t jacoco --exit-code $? || echo  “Skipping Code Climate coverage upload”
  #TODEL - ./cc-test-reporter after-build -d -t cobertura --exit-code $? || echo  “Skipping Code Climate coverage upload”


# Pipe the coverage data to Code Climate
#TODEL after_script:
#TODEL   - ./cc-test-reporter format-coverage -t simplecov -o coverage/codeclimate.backend.json coverage/backend/.resultset.json # Format backend coverage
#TODEL   - ./cc-test-reporter format-coverage -t lcov -o coverage/codeclimate.frontend.json coverage/frontend/lcov.info  # Format frontend coverage
#TODEL   - ./cc-test-reporter sum-coverage coverage/codeclimate.*.json -p 2                  # Sum both coverage parts into coverage/codeclimate.json
#TODEL   - if [[ "$TRAVIS_TEST_RESULT" == 0 ]]; then ./cc-test-reporter upload-coverage; fi  # Upload coverage/codeclimate.json

after_success:
#For Code Climate Test Coverage
  #Solution from https://github.com/codeclimate/test-reporter/issues/259#issuecomment-374280649
  #- JACOCO_SOURCE_PATH=src/main/java ./cc-test-reporter format-coverage target/site/jacoco/jacoco.xml --input-type jacoco
  - JACOCO_SOURCE_PATH=src/main/java ./cc-test-reporter format-coverage target/site/cobertura/coverage.xml --input-type cobertura
  - ./cc-test-reporter upload-coverage -d

#For Codecov Test Coverage
  - bash <(curl -s https://codecov.io/bash) || echo "Codecov did not collect coverage reports"
